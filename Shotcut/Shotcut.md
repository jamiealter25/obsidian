1. Install `Shotcut` from this link → https://www.shotcut.org/download/
2. Clean up Layout
   - Remove → `Recent, History, Keyframes` tabs cause you won't use them.
   - Also remove → `Show Toolbar, Show Text Under Icons`
   - DO NOT REMOVE → `Show Title Bars` it's not possible to drag tabs without it.
    ![[ShowTitleBars.png]]
3.  Make your own layout
   - Enable  → `Export, Jobs` tab.
   - Drag on drop 