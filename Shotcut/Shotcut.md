1. Install `Shotcut` from this link → https://www.shotcut.org/download/
2. Clean up interface
   - Remove tabs `Recent, History, Keyframes `
   - 
   - `Show Title Bars`, `Show Toolbar`, `Show Text Under Icons`
    ![[ShowTitleBars.png]]
   