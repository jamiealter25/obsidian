- Import SQLite3 library (library is pre-written code)
```Python
import sqlite3
```

- Connect your `.db` file with sqlite3 library with `.connect()` method
- Assign a variable to use this code later
```Python
whatever = sqlite3.connect('YourFileName.db')
```

- 